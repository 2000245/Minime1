<?php
// Проверка, был ли отправлен запрос
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Получение номера телефона из формы
    $phone = htmlspecialchars(trim($_POST['phone']));

    // Здесь вы можете добавить логику для проверки номера телефона
    // Например, проверка в базе данных, отправка SMS и т.д.
    
    // Пример: просто выводим номер телефона
    echo "На номер $phone отправлено сообщение для восстановления пароля.";
    
    // Здесь вы можете добавить код для отправки SMS или email с инструкциями по восстановлению пароля
} else {
    // Если форма не была отправлена, перенаправляем обратно на страницу входа
    header("Location: login.php");
    exit();
}
?>
